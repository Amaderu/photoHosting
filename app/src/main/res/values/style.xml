<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--РѕС‚РєР»СЋС‡РµРЅРёРµ Р·Р°РіРѕР»РѕРІРєР° Рё РІРµСЂС…РЅРµР№ РїРѕРґСЃРІРµС‚РєРё С€С‚РѕСЂРєРё СѓРІРµРґРѕРјР»РµРЅРёР№-->
        <!--<item name="colorPrimaryDark">@android:color/transparent</item>-->
        <item name="android:windowNoTitle">true</item>
        <item name="android:actionBarStyle">@style/ThemeActionBar</item>
        <item name="android:windowActionBarOverlay">true</item>
    </style>

    <style name="ThemeActionBar"
        parent="Widget.AppCompat.Light.ActionBar.Solid">
        <item name="android:background">@null</item>
        <!-- Support library compatibility -->
        <item name="background">@null</item>
    </style>

    <style name="whiteStyle" parent="TextAppearance.AppCompat.Button">
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">13sp</item>
        <!--<item name="android:font">@font/roboto</item>-->
        <!--<item name="android:background">#FFFFFF</item>-->
        <item name="android:textAlignment">center</item>
        <item name="android:background">@drawable/border_white</item>

    </style>

    <style name="blackStyle" parent="TextAppearance.AppCompat.Button">
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textSize">13sp</item>
        <!--<item name="android:font">@font/roboto</item>-->
        <item name="android:background">@drawable/border_black</item>
        <item name="android:textAlignment">center</item>
    </style>


</resources>
